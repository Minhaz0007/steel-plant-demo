services:
  - type: web
    name: steel-plant-api
    env: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    region: oregon
    plan: free
    envVars:
      - key: FRONTEND_URL
        value: https://your-app.vercel.app
      - key: PORT
        value: 8000
